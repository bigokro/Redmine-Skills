<% labelled_tabular_form_for :skill, @skill, :url => { :action => "edit", :id => @skill, :project_id => @project } do |f| %>
  <%= render :partial => 'form', :locals => { :f => f } %>
  <%= submit_tag l(:button_save), :tabindex => autotab %>
<% end %>


